#ifndef K2B_DISP_UAPI_H
#define K2B_DISP_UAPI_H

#include <stdbool.h>
#include <stdint.h>

typedef uint32_t u32;
typedef int32_t s32;

/* Use the matching vendor kernel header, supplied by the build environment. */
#include <video/sunxi_display2.h>

#endif
